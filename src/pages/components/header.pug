header
  .container
    .nav-left
      a.logo(href="#") 
        span Investfund
    .nav-center
      a.nav-link(href="#") Assets
      button.nav-link(href="#") My portfolio
    .nav-right
      button.sign-in(onClick="document.querySelector('#auth-popup').style.display = 'block';") Sign in
      button.sign-up(onClick="document.querySelector('#auth-popup').style.display = 'block';") Sign up
      //- .avatar
        svg(width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg")
